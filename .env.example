# Kawaiidra MCP Server Configuration
# Copy this file to .env and update the paths for your system

# OPTIONAL: Path to Ghidra installation directory
# If not set, the server will auto-detect Ghidra in common locations
#
# Manual installations:
#   Windows: GHIDRA_INSTALL_DIR=C:\ghidra_11.2_PUBLIC
#   Linux:   GHIDRA_INSTALL_DIR=/opt/ghidra
#   macOS:   GHIDRA_INSTALL_DIR=/Applications/ghidra_11.2_PUBLIC
#
# Homebrew installations (auto-detected, but can be set explicitly):
#   macOS (Apple Silicon): GHIDRA_INSTALL_DIR=/opt/homebrew
#   macOS (Intel):         GHIDRA_INSTALL_DIR=/usr/local
#   Linux:                 GHIDRA_INSTALL_DIR=/home/linuxbrew/.linuxbrew
#
# You can also point directly to the binary:
#   GHIDRA_INSTALL_DIR=/opt/homebrew/bin/ghidraRun
#
# GHIDRA_INSTALL_DIR=/path/to/ghidra

# OPTIONAL: Override default directories
# KAWAIIDRA_PROJECT_DIR=./projects
# KAWAIIDRA_BINARIES_DIR=./binaries
# KAWAIIDRA_EXPORTS_DIR=./exports
# KAWAIIDRA_LOG_DIR=./logs

# OPTIONAL: Performance tuning
# KAWAIIDRA_TIMEOUT=300
# KAWAIIDRA_DECOMPILE_TIMEOUT=180

# OPTIONAL: Caching settings
# Caching dramatically speeds up repeated queries on the same binaries
# KAWAIIDRA_CACHE_ENABLED=true
# KAWAIIDRA_CACHE_DIR=~/.kawaiidra/cache
# KAWAIIDRA_CACHE_MAX_SIZE_MB=500

# OPTIONAL: JPype Bridge settings (100-1000x faster operations!)
# The bridge keeps a JVM running in-process, eliminating startup overhead.
# Requires: pip install JPype1  and  Java JDK 17+
#
# Enable/disable the bridge (falls back to subprocess if disabled or unavailable)
# KAWAIIDRA_USE_BRIDGE=true
#
# Keep loaded programs in memory for instant subsequent operations
# KAWAIIDRA_BRIDGE_CACHE_PROGRAMS=true
#
# Maximum number of programs to keep cached in memory (LRU eviction)
# KAWAIIDRA_BRIDGE_MAX_PROGRAMS=5
